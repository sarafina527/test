public class Solution {
    public int calculate(String s) {
        LinkedList<Integer> stack = LinkedList<Integer>();
        for(int i=0;i<s.length();i++){
        	char c = s.charAt(i);
        	if(c==' ') continue;
        	if(c=='('){

        	}
        }
    }
}